[pytest]
testpaths = tests
addopts = -q --tb=short
filterwarnings =
    ignore::PendingDeprecationWarning
    ignore::DeprecationWarning
markers =
    network: requires live network access to an external data provider
    slow: runs a full simulation and takes tens of seconds
